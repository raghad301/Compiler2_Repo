<body>
    <h1>{{ title }}</h1>

    {% set p_name = "Samsung S23" %}
    {% set status = "active" %}
    {% set base_stock = 150 %}

    <div class="main-container">
        {% if status == "active" %}
            <div class="item-card">
                <h2>Product: {{ p_name }}</h2>

                {% set local_discount = 0.10 %}
                <p>Discount Applicable: {{ local_discount }}</p>
            </div>
        {% else %}
            <p>Status is offline for {{ title }}</p>
        {% endif %}

        <div class="pricing-matrix">
            {% set original_price = 2000.0 %}
            {% set tax_rate = 0.05 %}

            {% set final_price = original_price * tax_rate %}
            <span>Calculated Tax Amount: {{ final_price }}</span>
        </div>

        <div class="catalog-grid">
            <h3>Available Store Items:</h3>
            {% for product in products %}
                <div class="grid-item">
                    <p>Item Name: {{ product }}</p>
                </div>
            {% endfor %}
        </div>
    </div>
</body>